local syn = getgenv().syn
getgenv().syn = false

local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/shlexware/Orion/main/source')))() local Window = OrionLib:MakeWindow({Name = "JLhub", HidePremium = false, SaveConfig = true, ConfigFolder = "OrionTest"}) local Tab = Window:MakeTab({
    Name = "auto farm",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
}) Tab:AddButton({
    Name = "auto strength (use any tool or bench)",
    Callback = function()
              _G.toggle = true

while _G.toggle do
wait( )
local args = {
    [1] = "rep"
}

game:GetService("Players").LocalPlayer.muscleEvent:FireServer(unpack(args))
end
      end    
}) Tab:AddButton({
    Name = "turn off auto strength",
    Callback = function()
              _G.toggle = false

while _G.toggle do
wait( )
local args = {
    [1] = "rep"
}

game:GetService("Players").LocalPlayer.muscleEvent:FireServer(unpack(args))
end
      end    
})local Tab = Window:MakeTab({
    Name = "misc",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
}) Tab:AddButton({
    Name = "walk and jump while train",
    Callback = function()
              while wait() do
game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = 500

game.Players.LocalPlayer.Character.Humanoid.JumpPower = 200
end
      end    
}) Tab:AddButton({
    Name = "infinite jump",
    Callback = function()
              
--script made by Youtube: MrGabbe.   Discord: Mr_G_Curry #6982
local infjmp = true
game:GetService("UserInputService").jumpRequest:Connect(function()
    if infjmp then
        game:GetService"Players".LocalPlayer.Character:FindFirstChildOfClass"Humanoid":ChangeState("Jumping")
    end
end)
      end    
})local Tab = Window:MakeTab({
    Name = "chests",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
}) Tab:AddButton({
    Name = "claim legend chest",
    Callback = function()
              
local args = {
    [1] = "Legends Chest"
}

game:GetService("ReplicatedStorage").rEvents.checkChestRemote:InvokeServer(unpack(args))

      end    
}) Tab:AddButton({
    Name = "claim mythical chest",
    Callback = function()
              
local args = {
    [1] = "Mythical Chest"
}

game:GetService("ReplicatedStorage").rEvents.checkChestRemote:InvokeServer(unpack(args))

      end    
}) Tab:AddButton({
    Name = "claim magma chest",
    Callback = function()
              local args = {
    [1] = "Magma Chest"
}

game:GetService("ReplicatedStorage").rEvents.checkChestRemote:InvokeServer(unpack(args))

      end    
}) Tab:AddButton({
    Name = "claim enchanted chest",
    Callback = function()
              local args = {
    [1] = "Enchanted Chest"
}

game:GetService("ReplicatedStorage").rEvents.checkChestRemote:InvokeServer(unpack(args))

      end    
}) Tab:AddButton({
    Name = "claim golden chest",
    Callback = function()
              local args = {
    [1] = "Golden Chest"
}

game:GetService("ReplicatedStorage").rEvents.checkChestRemote:InvokeServer(unpack(args))

      end    
}) local Tab = Window:MakeTab({
    Name = "combat",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
}) Tab:AddButton({
    Name = "auto punch part 1",
    Callback = function()
              while wait() do
local args = {
    [1] = "punch",
    [2] = "leftHand"
}

game:GetService("Players").LocalPlayer.muscleEvent:FireServer(unpack(args))
end
      end    
}) Tab:AddButton({
    Name = "auto punch part 2",
    Callback = function()
              while wait() do
local args = {
    [1] = "punch",
    [2] = "rightHand"
}

game:GetService("Players").LocalPlayer.muscleEvent:FireServer(unpack(args))
end
      end    
}) Tab:AddButton({
    Name = "auto groundslam",
    Callback = function()
              while wait(0.9) do
local args = {
    [1] = "groundSlam"
}

game:GetService("Players").LocalPlayer.muscleEvent:FireServer(unpack(args))
end
      end    
}) Tab:AddButton({
    Name = "auto stomp",
    Callback = function()
              while wait(0.9) do
local args = {
    [1] = "stomp"
}

game:GetService("Players").LocalPlayer.muscleEvent:FireServer(unpack(args))
end
      end    
}) local Tab = Window:MakeTab({
    Name = "size changers",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
}) Tab:AddButton({
    Name = "micro (secret size",
    Callback = function()
              local args = {
    [1] = "changeSize",
    [2] = 0/0 --[[NaN]]
}

game:GetService("ReplicatedStorage").rEvents.changeSpeedSizeRemote:InvokeServer(unpack(args))

      end    
}) Tab:AddButton({
    Name = "avarge (2)",
    Callback = function()
              local args = {
    [1] = "changeSize",
    [2] = 2
}

game:GetService("ReplicatedStorage").rEvents.changeSpeedSizeRemote:InvokeServer(unpack(args))

      end    
}) Tab:AddButton({
    Name = "your current max size",
    Callback = function()
              local args = {
    [1] = "changeSize",
    [2] = 99999
}

game:GetService("ReplicatedStorage").rEvents.changeSpeedSizeRemote:InvokeServer(unpack(args))

      end    
}) Tab:AddButton({
    Name = "giant (30)",
    Callback = function()
              local args = {
    [1] = "changeSize",
    [2] = 30
}

game:GetService("ReplicatedStorage").rEvents.changeSpeedSizeRemote:InvokeServer(unpack(args))

      end    
}) Tab:AddButton({
    Name = "omega (50)",
    Callback = function()
              local args = {
    [1] = "changeSize",
    [2] = 50
}

game:GetService("ReplicatedStorage").rEvents.changeSpeedSizeRemote:InvokeServer(unpack(args))

      end    
}) Tab:AddButton({
    Name = "titan",
    Callback = function()
              local args = {
    [1] = "changeSize",
    [2] = 70
}

game:GetService("ReplicatedStorage").rEvents.changeSpeedSizeRemote:InvokeServer(unpack(args))

      end    
}) Tab:AddButton({
    Name = "the end (100)",
    Callback = function()
              local args = {
    [1] = "changeSize",
    [2] = 100
}

game:GetService("ReplicatedStorage").rEvents.changeSpeedSizeRemote:InvokeServer(unpack(args))

      end    
}) local Tab = Window:MakeTab({
    Name = "rebirth system",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
}) Tab:AddButton({
    Name = "auto rebirth",
    Callback = function()
              _G.toggle = true

while _G.toggle do
wait( )
local args = {
    [1] = "rebirthRequest"
}

game:GetService("ReplicatedStorage").rEvents.rebirthRemote:InvokeServer(unpack(args))
end
      end    
}) Tab:AddButton({
    Name = "turn off auto rebirth",
    Callback = function()
              _G.toggle = false

while _G.toggle do
wait( )
local args = {
    [1] = "rebirthRequest"
}

game:GetService("ReplicatedStorage").rEvents.rebirthRemote:InvokeServer(unpack(args))
end
      end    
}) local Tab = Window:MakeTab({
	Name = "teleports",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
}) Tab:AddButton({
	Name = "tween to main island",
	Callback = function()
      		local teleport_table = {
    location1 = Vector3.new(-47.257972717285156, 209.99107360839844, 93.2047348022461), -- your desired position
    location2 = Vector3.new(-47.257972717285156, 209.99107360839844, 93.2047348022461)  -- your desired position
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
  	end    
}) Tab:AddButton({
	Name = "tween to legends gym (best)",
	Callback = function()
      		local teleport_table = {
    location1 = Vector3.new(4349.4892578125, 1083.158935546875, -3853.33935546875), -- your desired position
    location2 = Vector3.new(4349.4892578125, 1083.158935546875, -3853.33935546875)  -- your desired position
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
  	end    
}) Tab:AddButton({
	Name = "tween to lava gym",
	Callback = function()
      		local teleport_table = {
    location1 = Vector3.new(-6940.2763671875, 558.2366333007812, -1228.9951171875), -- your desired position
    location2 = Vector3.new(-6940.2763671875, 558.2366333007812, -1228.9951171875)  -- your desired position
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
  	end    
}) Tab:AddButton({
	Name = "tween to mythic gym",
	Callback = function()
      		local teleport_table = {
    location1 = Vector3.new(2492.466064453125, 599.205322265625, 894.744140625), -- your desired position
    location2 = Vector3.new(2492.466064453125, 599.205322265625, 894.744140625)  -- your desired position
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
  	end    
}) Tab:AddButton({
	Name = "tween to enchantment world",
	Callback = function()
      		local teleport_table = {
    location1 = Vector3.new(-2799.79345703125, 778.7362060546875, -339.0997009277344), -- your desired position
    location2 = Vector3.new(-2799.79345703125, 778.7362060546875, -339.0997009277344)  -- your desired position
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
  	end    
}) local Tab = Window:MakeTab({
	Name = "remove stuff",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
}) Tab:AddButton({
	Name = "remove game teleporter ( HIGHLY IMPORTANT)",
	Callback = function()
      		game.workspace.RobloxForwardPortals:destroy()
  	end    
})  local Tab = Window:MakeTab({
	Name = "READ ME (IMP)",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
}) Tab:AddButton({
	Name = "before using auto farm rock you need to know those",
	Callback = function()
      		print("button pressed")
  	end    
}) Tab:AddButton({
	Name = "require (number) is the amount of durability you need",
	Callback = function()
      		print("button pressed")
  	end    
}) Tab:AddButton({
	Name = "turn on auto punch part 1 and part 2 before using these",
	Callback = function()
      		print("button pressed")
  	end    
}) Tab:AddButton({
	Name = "remove portal in the remove stuff feature to afoid flying into one",
	Callback = function()
      		print("button pressed")
  	end    
}) local Tab = Window:MakeTab({
	Name = "stop tweens",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
}) Tab:AddButton({
	Name = "stop all auto tweens",
	Callback = function()
      		_G.tweening = false

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(4111.87841796875, 1028.8409423828125, -4102.58447265625), 
    location2 = Vector3.new(4111.87841796875, 1028.8409423828125, -4102.58447265625)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.7)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(4106.81201171875, 1022.899169921875, -4072.775146484375), 
    location2 = Vector3.new(4106.81201171875, 1022.899169921875, -4072.775146484375)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.8)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(4129.99609375, 1021.000732421875, -4048.4267578125), 
    location2 = Vector3.new(4129.99609375, 1021.000732421875, -4048.4267578125)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.9)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(4145.056640625, 1019.2320556640625, -4048.6533203125), -- your desired position
    location2 = Vector3.new(4145.056640625, 1019.2320556640625, -4048.6533203125)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(1)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(4160.76904296875, 1029.478271484375, -4066.618408203125), 
    location2 = Vector3.new(4160.76904296875, 1029.478271484375, -4066.618408203125)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(1.1)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(4164.33447265625, 1032.61865234375, -4099.30712890625), 
    location2 = Vector3.new(4164.33447265625, 1032.61865234375, -4099.30712890625)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(1.2)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(4140.21337890625, 1026.02880859375, -4123.99755859375), 
    location2 = Vector3.new(4140.21337890625, 1026.02880859375, -4123.99755859375)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(1.3)
end
end)
  	end    
}) local Tab = Window:MakeTab({
	Name = "kings rock",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
}) Tab:AddButton({
	Name = "auto punch kings rock",
	Callback = function()
      		_G.tweening = true

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(-8922.11328125, 57.20711898803711, -6019.61669921875), 
    location2 = Vector3.new(-8922.11328125, 57.20711898803711, -6019.61669921875)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(0.1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.5)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(-8928.59375, 82.05540466308594, -6067.51904296875), 
    location2 = Vector3.new(-8995.4013671875, 77.38914489746094, -6012.244140625)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(0.1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.6)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(-8995.4013671875, 77.38914489746094, -6012.244140625), 
    location2 = Vector3.new(-8995.4013671875, 77.38914489746094, -6012.244140625)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(0.1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.7)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(-8995.193359375, 86.72904205322266, -6066.5048828125), -- your desired position
    location2 = Vector3.new(-8995.193359375, 86.72904205322266, -6066.5048828125)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(0.1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.8)
end
end)
  	end    
})  local Tab = Window:MakeTab({
	Name = "legends rock",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
}) Tab:AddButton({
	Name = "auto farm legends rock 1m dura required",
	Callback = function()
      		_G.tweening = true

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(4111.87841796875, 1028.8409423828125, -4102.58447265625), 
    location2 = Vector3.new(4111.87841796875, 1028.8409423828125, -4102.58447265625)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.7)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(4106.81201171875, 1022.899169921875, -4072.775146484375), 
    location2 = Vector3.new(4106.81201171875, 1022.899169921875, -4072.775146484375)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.8)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(4129.99609375, 1021.000732421875, -4048.4267578125), 
    location2 = Vector3.new(4129.99609375, 1021.000732421875, -4048.4267578125)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.9)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(4145.056640625, 1019.2320556640625, -4048.6533203125), -- your desired position
    location2 = Vector3.new(4145.056640625, 1019.2320556640625, -4048.6533203125)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(1)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(4160.76904296875, 1029.478271484375, -4066.618408203125), 
    location2 = Vector3.new(4160.76904296875, 1029.478271484375, -4066.618408203125)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(1.1)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(4164.33447265625, 1032.61865234375, -4099.30712890625), 
    location2 = Vector3.new(4164.33447265625, 1032.61865234375, -4099.30712890625)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(1.2)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(4140.21337890625, 1026.02880859375, -4123.99755859375), 
    location2 = Vector3.new(4140.21337890625, 1026.02880859375, -4123.99755859375)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(1.3)
end
end)
  	end    
}) local Tab = Window:MakeTab({
	Name = "lava rock",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
}) Tab:AddButton({
	Name = "auto farm lava rock 750k required",
	Callback = function()
      		_G.tweening = true

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(-7237.39404296875, 31.668691635131836, -1256.734619140625), 
    location2 = Vector3.new(-7237.39404296875, 31.668691635131836, -1256.734619140625)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(0.1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.5)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(-7252.4638671875, 32.033447265625, -1285.5211181640625), 
    location2 = Vector3.new(-7252.4638671875, 32.033447265625, -1285.5211181640625)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(0.1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.6)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(-7258.8701171875, 27.567041397094727, -1229.9302978515625), 
    location2 = Vector3.new(-7258.8701171875, 27.567041397094727, -1229.9302978515625)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(0.1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.7)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(-7249.53466796875, 36.063087463378906, -1251.8638916015625), -- your desired position
    location2 = Vector3.new(-7249.53466796875, 36.063087463378906, -1251.8638916015625)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(0.1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.8)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(-7281.5595703125, 28.9803466796875, -1238.6187744140625), -- your desired position
    location2 = Vector3.new(-7281.5595703125, 28.9803466796875, -1238.6187744140625)  -- your desired position
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.9)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(-7286.7314453125, 32.586021423339844, -1254.263671875), -- your desired position
    location2 = Vector3.new(-7286.7314453125, 32.586021423339844, -1254.263671875)  -- your desired position
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(1)
end
end)
  	end    
}) local Tab = Window:MakeTab({
	Name = "mythic rock",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
}) Tab:AddButton({
	Name = "auto punch mythic rock 400k required",
	Callback = function()
      		_G.tweening = true

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(2172.7373046875, 29.503931045532227, 1277.8480224609375), 
    location2 = Vector3.new(2172.7373046875, 29.503931045532227, 1277.8480224609375)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(0.1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.45)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(2195.650634765625, 33.163753509521484, 1278.59326171875), 
    location2 = Vector3.new(-2195.650634765625, 33.163753509521484, 1278.59326171875)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(0.1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.6)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(2196.684326171875, 34.125343322753906, 1223.0218505859375), 
    location2 = Vector3.new(2196.684326171875, 34.125343322753906, 1223.0218505859375)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(0.1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.7)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(2172.822998046875, 34.04256820678711, 1223.6778564453125), -- your desired position
    location2 = Vector3.new(2172.822998046875, 34.04256820678711, 1223.6778564453125)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(0.1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.8)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(2183.917724609375, 44.49549865722656, 1246.8096923828125), -- your desired position
    location2 = Vector3.new(2183.917724609375, 44.49549865722656, 1246.8096923828125)  -- your desired position
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.9)
end
end)
  	end    
}) local Tab = Window:MakeTab({
	Name = "enchanted rock",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
}) Tab:AddButton({
	Name = "auto punch enchanted rock 150k required",
	Callback = function()
      		_G.tweening = true

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(-2560.86474609375, 33.5127067565918, -265.8412780761719), 
    location2 = Vector3.new(-2560.86474609375, 33.5127067565918, -265.8412780761719)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(0.1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.35)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(-2542.48974609375, 30.737279891967773, -267.9456481933594), 
    location2 = Vector3.new(-2542.48974609375, 30.737279891967773, -267.9456481933594)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(0.1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.5)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(-2538.37158203125, 31.928268432617188, -219.12054443359375), 
    location2 = Vector3.new(-2538.37158203125, 31.928268432617188, -219.12054443359375)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(0.1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.6)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(-2561.3271484375, 32.05541229248047, -217.08700561523438), -- your desired position
    location2 = Vector3.new(-2561.3271484375, 32.05541229248047, -217.08700561523438)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(0.1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.7)
end
end)
  	end    
}) local Tab = Window:MakeTab({
	Name = "gold rock",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
}) Tab:AddButton({
	Name = "auto punch gold rock 5k required",
	Callback = function()
      		_G.tweening = true

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(296.872314453125, 16.967180252075195, -569.5556030273438), 
    location2 = Vector3.new(296.872314453125, 16.967180252075195, -569.5556030273438)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(0.1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.3)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(316.7738952636719, 16.140090942382812, -566.7810668945312), 
    location2 = Vector3.new(316.7738952636719, 16.140090942382812, -566.7810668945312)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(0.1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.5)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(310.30792236328125, 20.540258407592773, -617.0482788085938), 
    location2 = Vector3.new(310.30792236328125, 20.540258407592773, -617.0482788085938)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(0.1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.6)
end
end)
  	end    
}) local Tab = Window:MakeTab({
	Name = "big rock",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
}) Tab:AddButton({
	Name = "auto punch big rock requires 100",
	Callback = function()
      		_G.tweening = true

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(163.70179748535156, 11.583303451538086, -140.2444305419922), 
    location2 = Vector3.new(163.70179748535156, 11.583303451538086, -140.2444305419922)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(0.1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.3)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(157.06455993652344, 7.880949974060059, -163.62481689453125), 
    location2 = Vector3.new(157.06455993652344, 7.880949974060059, -163.62481689453125)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(0.1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.5)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(177.67198181152344, 9.99731159210205, -158.81024169921875), 
    location2 = Vector3.new(177.67198181152344, 9.99731159210205, -158.81024169921875)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(0.1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.6)
end
end)
  	end    
}) local Tab = Window:MakeTab({
	Name = "small rock",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
}) Tab:AddButton({
	Name = "auto punch small rock requires 10",
	Callback = function()
      		_G.tweening = true

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(-161.24911499023438, 12.515350341796875, 416.2306213378906), 
    location2 = Vector3.new(-161.24911499023438, 12.515350341796875, 416.2306213378906)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(0.1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.12)
end
end)

spawn(function( )
while _G.tweening == true do
local teleport_table = {
    location1 = Vector3.new(-139.8083038330078, 10.452432632446289, 418.75909423828125), 
    location2 = Vector3.new(-139.8083038330078, 10.452432632446289, 418.75909423828125)  
}

local tween_s = game:GetService('TweenService')
local tweeninfo = TweenInfo.new(0.1,Enum.EasingStyle.Linear)

local lp = game.Players.LocalPlayer

function bypass_teleport(v)
    if lp.Character and 
    lp.Character:FindFirstChild('HumanoidRootPart') then
        local cf = CFrame.new(v)
        local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
        a:Play()
        -- a.Completed:Wait()
        -- print('Teleporting Done!')
    end
end

bypass_teleport(teleport_table.location1)
wait(0.2)
end
end)
  	end    
})

getgenv().syn = syn
